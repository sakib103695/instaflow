# Avoid ERESOLVE peer dependency conflicts and npm resolver bugs on Hostinger
legacy-peer-deps=true

# Prefer exact install (helps avoid "edgesOut" / "matches" resolver bugs)
prefer-dedupe=true

# Don't fail on engine version mismatch (Hostinger may use different Node)
engine-strict=false
